import { NextPage } from "next";

type IndexPageProps = string;

const IndexPage: NextPage<IndexPageProps> = (props): JSX.Element => (
  <div style={{ textAlign: 'center' }}>
  </div>
);

export default IndexPage;
